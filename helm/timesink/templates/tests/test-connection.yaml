apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "timesink.fullname" . }}-test-connection"
  labels:
{{ include "timesink.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "timesink.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
